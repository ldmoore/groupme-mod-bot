{
	"name": "groupme-mod-bot",
	"version": "1.0.0",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format .",
		"format:fix": "biome format --write .",
		"test": "jest",
		"test:coverage": "jest --coverage",
		"test:coverage:ci": "jest --coverage --ci",
		"check": "node --import tsx test/blockCheck.ts"
	},
	"dependencies": {
		"hono": "^4.6.20"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.0",
		"@types/jest": "^30.0.0",
		"@types/node": "22.13.0",
		"@types/service-worker-mock": "^2.0.4",
		"jest": "^29.7.0",
		"ts-jest": "^29.4.5",
		"tsx": "^4.20.6",
		"wrangler": "^4.32.0"
	}
}
